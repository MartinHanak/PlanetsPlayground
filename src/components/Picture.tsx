interface pictureProps {
    sources: string[],
    widths: number[],
    alt: string
}

export default function Picture({ sources, widths, alt }: pictureProps) {

    return (
        <picture>

        </picture>
    )
}